const usernames1: string[] = [];
function greetUsers(users: string[]): void {
  if (users.length === 0) {
    console.log("We need to find some users!");
    return; 
  }
}

greetUsers(usernames1); 
